openapi: 3.0.3
info:
  title: Example API
  version: 0.1.0
  description: |
    Minimal OpenAPI stub for Flutterozavr skeleton.
    Replace with your backend contract when integrating a real API.
paths:
  /api/v1/health:
    get:
      operationId: health_check
      summary: Health check
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
